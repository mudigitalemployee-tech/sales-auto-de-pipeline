# Sales Auto — Data Engineering Pipeline

> Generated by **Ved** — Mu Sigma Digital Employee 🧠

## Overview

| Field | Value |
|---|---|
| **Purpose** | 10-minute auto-refresh sales pipeline |
| **Source** | CSV |
| **Raw Rows** | 15 |
| **Silver Rows** | 15 |
| **Gold Rows** | 15 |
| **DQ Score** | 100.0% |
| **Schedule** | 0 6 * * * |
| **DAG ID** | sales_auto_pipeline |

## Repository Structure

```
data/
  bronze_raw.csv            ← Raw source data exactly as ingested (Bronze layer)
  transformed.csv          ← Cleaned, deduped, PII-masked data (Silver layer)
  gold_aggregated.csv       ← Business KPI aggregations (Gold layer)
  dq_scorecard.json         ← Data quality check results (96.7% score)
  transform_log.json        ← Full audit trail of all transformations
pipeline/
  airflow_dag.py            ← Airflow DAG (schedule: 0 6 * * *)
  transform.py              ← Silver layer transformation logic
  dq_checks.py              ← Data quality validation checks
reports/
  *.html                    ← MuSigma interactive HTML report
  *.zip                     ← Full bundle (HTML + CSVs + DAG)
.github/workflows/
  pipeline.yml              ← CI/CD: lint → validate → deploy
README.md
requirements.txt
```

## Pipeline Architecture

```
Source CSV
    ↓
[Bronze Layer]  — Raw ingestion, no changes
    ↓
[Silver Layer]  — Clean, dedup, type-cast, null-handle, PII-mask
    ↓
[Gold Layer]    — Business aggregations (KPIs for dashboard)
    ↓
[Report]        — MuSigma HTML report + ZIP delivery
```

## Running the Pipeline

```bash
pip install -r requirements.txt
# Run manually
python3 pipeline/airflow_dag.py

# Run DQ checks
python3 pipeline/dq_checks.py --data_path data/transformed.csv
```

## CI/CD

Runs automatically on every push to `main`:
1. **lint** — flake8 on all pipeline code
2. **validate** — row count check on silver data

---
*Pipeline generated on 2026-04-08 by Ved DE Skill v2.0*
# Auto-refresh enabled
